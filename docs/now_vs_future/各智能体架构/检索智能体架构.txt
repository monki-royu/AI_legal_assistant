                    ┌──────────────────────────┐
                    │  检索入口节点             │
                    │  retrieval_intent_decompose│
                    │  接收 state 中的:          │
                    │  - retrieval_query        │
                    │  - retrieval_keywords     │
                    │  - task_type              │
                    │  - contract_type          │
                    │  - mounted_sources        │
                    │  - skip_fusion            │
                    │  - doc_text (企查查用)    │
                    └──────────┬───────────────┘
                               │
                               ▼
                    ┌──────────────────────────┐
                    │  意图分解(关键词扩展)     │
                    │  eg. "违约金" →            │
                    │  ["违约金","违约赔偿",    │
                    │   "损害赔偿","合同违约"]  │
                    └──────────┬───────────────┘
                               │
          ╔════════════════════╪═══════════════════════════╗
          ║                     ▼                          ║
          ║    ┌────────── 检索基础层节点 ──────────┐      ║
          ║    │  根据 mounted_sources 过滤数据源     │      ║
          ║    │                                      │      ║
          ║    │  L0 (laws): Neo4j 实体匹配           │      ║
          ║    │    ↓ 不足3条                          │      ║
          ║    │  L1 (laws): FAISS 向量检索            │      ║
          ║    │    ↓ 不足3条                          │      ║
          ║    │  L2 (laws): 本地法规关键词扫描        │      ║
          ║    │     ├ L2.1 精确匹配                   │      ║
          ║    │     ├ L2.2 时效过滤(标注已废止)       │      ║
          ║    │     ├ L2.3 模糊匹配(2字滑窗)          │      ║
          ║    │     └ L2.4 单字兜底                   │      ║
          ║    │                                      │      ║
          ║    │  L3 (industry): 行业标准检索          │      ║
          ║    │     ├ L3.0 编号精确匹配(GB/JGJ)      │      ║
          ║    │     ├ L3.1a 关键词精确匹配            │      ║
          ║    │     ├ L3.1b 模糊匹配(2字滑窗)         │      ║
          ║    │     └ L3.2 FAISS 语义降级             │      ║
          ║    │                                      │      ║
          ║    │  L4 (cases): 案例检索                 │      ║
          ║    │     ├ L4.0 Neo4j Case 节点实体匹配    │      ║
          ║    │     └ L4.3 LLM 类案总结(兜底)         │      ║
          ║    │                                      │      ║
          ║    │  L5 (interpretations): 司法解释       │      ║
          ║    │     ├ 编号匹配→关键词→FAISS 降级      │      ║
          ║    │                                      │      ║
          ║    │  共享: 企查查资信(从 doc_text 提取     │      ║
          ║    │        企业名→MCP→AppKey→Mock)       │      ║
          ║    └────────────────┬─────────────────────┘      ║
          ║                     ▼ base_citations            ║
          ║    ┌────────── 检索增强查询节点 ──────┐         ║
          ║    │  LLM伪检索: 当 base_citations    │         ║
          ║    │  不足时,由LLM直接生成相关内容     │         ║
          ║    └────────────────┬─────────────────┘         ║
          ║                     ▼ enhance_citations          ║
          ║    ┌────────── 融合排序节点 ───────────┐        ║
          ║    │  skip_fusion=True?                      ║  ║
          ║    │   ├─ Yes → 直接返回(单源直查)          ║  ║
          ║    │   └─ No  → RRF 融合(倒数排名)          ║  ║
          ║    │            + 去重 + 冲突消解            ║  ║
          ║    │            + 质量分计算                 ║  ║
          ║    └────────────────┬──────────────────────┘  ║
          ║                     ▼ citations               ║
          ║    ┌────────── 质量门禁节点 ──────────┐       ║
          ║    │  quality_score >= 0.85 ?              ║  ║
          ║    │   ├─ Yes ✅ 返回结果，跳过 MCP       ║  ║
          ║    │   └─ No  → quality_retry_count++    ║  ║
          ║    │            retry < 3 ?              ║  ║
          ║    │              ├ Yes 🔄 回退重试       ║  ║
          ║    │              └ No  → 人类介入提示    ║  ║
          ║    │                       ┌─ 用户确认     ║  ║
          ║    │                       │  → 北大法宝   ║  ║
          ║    │                       │    MCP 兜底   ║  ║
          ║    │                       └─ 用户拒绝     ║  ║
          ║    │                          → 返回现有   ║  ║
          ║    └─────────────────────────────────────┘  ║
          ╚═══════════════════════════════════════════════╝