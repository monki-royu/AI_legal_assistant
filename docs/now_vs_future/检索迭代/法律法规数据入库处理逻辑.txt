__002__extract_information/
├── __000__extract_graph_data_utils.py   ← 已有: 图数据工具函数
├── __001__extract_law_data.py           ← 已有: 批量 LLM 抽取(带断点续跑)
└── entity_extractor.py                  ← 【新建】统一抽取接口
     ├── extract_entities(text)          → 主接口: LLM → 规则 → 兜底
     ├── merge_results(results)          → 去重合并多次抽取结果
     ├── _extract_by_rules(text)         → 规则降级(正则+词典)
     └── _save_finetune_data(...)        → 自动保存微调数据到 data/extract_finetune_data.jsonl

__003__create_neo4j_database/
├── __001__graph_importer.py             ← 已有: Neo4j 批量写入
├── __002__export_metadata.py            ← 已有: 元数据导出
├── __003__vector_index.py               ← 已有: 向量索引
├── neo4j_client.py                      ← 【新建】Neo4j 连接封装
│    ├── health_check()                  → 检查连接可用性
│    ├── run_cypher(cypher, params)      → 执行+重试
│    ├── run_in_tx(queries)              → 批量事务写入
│    └── explain(cypher)                 → EXPLAIN 语法校验
├── cypher_generator.py                  ← 【新建】Cypher 生成+校验
│    ├── generate_create_cypher()        → 实体/关系 → CREATE Cypher
│    └── generate_match_cypher()         → 实体名列表 → MATCH Cypher
├── importer.py                          ← 【新建】完整导入流水线
│    └── run_pipeline(resume=True/False) → TXT→抽取→Cypher→Neo4j
└── query_node.py                        ← 【新建】LangGraph 查询节点
     └── neo4j_query_node(state)         → 用户问题→实体→Cypher→Neo4j→结果



法律法规 TXT
    ↓
__002__/entity_extractor.extract_entities()
    ├─ LLM 抽取 (第1级, 高精度)  ← 保存微调数据到 JSONL
    ├─ 规则抽取 (第2级, 正则+词典)
    └─ 降级兜底 (第3级, 返回基本结构)
    ↓  {entities: [...], relations: [...]}
__003__/cypher_generator.generate_create_cypher()
    ├─ LLM 生成 Cypher
    └─ 规则模板生成 (降级)
    ↓  Cypher 语句字符串
__003__/neo4j_client.explain()  ← 语法校验
    ↓  通过
__003__/neo4j_client.run_in_tx()  ← 写入 Neo4j
    ↓
进度记录 → data/neo4j_import_progress.json