用户问题 → Intent Router
            ├─ 意图锁定(专门页面) → 单源直查 → 跳过 RRF 融合
            └─ 意图未锁定(首页/智能体) → 全源挂载
                                            ↓
            ┌───────── 横向 5 源并行挂载 ─────────┐
            │ L0/L1/L2: 法规(实体匹配→FAISS→关键词) │
            │ L3:      行业标准(编号→关键词→FAISS) │
            │ L4:      案例(实体匹配→LLM兜底)       │
            │ L5:      司法解释(编号→关键词→FAISS)  │
            │ 共享:    企查查资信(MCP→Mock)         │
            └──────────────────────────────────────┘
                                            ↓
                                      RRF 融合
                                    冲突消解 4 规则
                                    质量门禁 ≥ 0.85 ?
                                                   ↓
                              Fusion 节点计算 quality_score
                                                ↓
                               quality_score >= 0.85 ?
                             ├─ Yes → 达标！直接返回结果 ✅
                            │          (跳过北大法宝 MCP，节省成本)
                            └─ No  → quality_retry_count += 1
                                                  ↓
                                        retry_count < 3 ?
                                    ├─ Yes → 重试（调整实体/关键词）🔄
                                    └─ No  → human_intervention_needed = True ⚠️
                                                             ↓
                                                        提示用户"是否调用北大法宝 MCP？"
                                                               ↓
                                                 用户确认 → get_beida_mcp_client().search_all()
                                                             ↓
                                                         MCP 结果返回 ✅
                                                                        ↓
                                                 用户拒绝 → 返回现有低质量结果（不调用付费 API）